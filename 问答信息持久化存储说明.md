# 问答信息持久化存储说明

## 功能说明

已为问答页面添加本地持久化存储功能，所有问答/通知数据将保存到微信小程序本地存储中。

## 实现方式

### 1. 数据存储位置
- **存储键名**：`notices`
- **存储方式**：`wx.setStorageSync()` 和 `wx.getStorageSync()`
- **数据格式**：JSON 数组

### 2. 新增功能

#### 加载数据时优先读取本地存储
```javascript
loadNotices() {
  const savedNotices = wx.getStorageSync('notices')
  const notices = savedNotices || initialNotices
  // ...
}
```

#### 自动保存数据
所有数据修改操作都会自动触发保存：
- 发布新通知 → 保存到本地
- 编辑通知 → 保存到本地
- 删除通知 → 保存到本地
- 标记已读 → 保存到本地

#### 新增清空功能
```javascript
clearAllNotices() {
  // 清空所有通知并删除本地存储
  wx.removeStorageSync('notices')
}
```

## 数据结构

每个通知包含以下字段：

```javascript
{
  id: '时间戳字符串',
  title: '通知标题',
  type: 'course|system|homework|grade',
  courseName: '关联课程名称（可选）',
  content: '通知内容',
  publishTime: '2024-11-15 10:30',
  isQuestion: true|false,
  isRead: true|false
}
```

## 使用场景

### 1. 首次使用
- 自动加载 4 条初始示例数据
- 数据保存到本地存储

### 2. 后续使用
- 优先读取本地存储的数据
- 保持数据一致性

### 3. 数据清理
- 在开发者工具控制台执行：
  ```javascript
  wx.clearStorageSync()  // 清空所有本地数据
  wx.removeStorageSync('notices')  // 仅清空通知数据
  ```

## 优势

1. **离线可用**：无需网络连接即可查看历史数据
2. **快速加载**：本地读取速度快
3. **数据安全**：数据存储在用户设备本地
4. **简单可靠**：无需配置云数据库

## 注意事项

1. **存储限制**：微信小程序本地存储有 10MB 限制
2. **数据同步**：多设备间数据不会自动同步
3. **数据清理**：清理小程序缓存会丢失本地数据
4. **备份建议**：重要数据建议定期备份或使用云存储

## 测试验证

### 测试步骤
1. 发布一条新通知
2. 关闭小程序
3. 重新打开小程序
4. 验证通知数据仍然存在

### 清理数据测试
```javascript
// 在开发者工具控制台执行
wx.clearStorageSync()
// 刷新页面，验证数据已重置为初始状态
```

## 未来扩展

如需实现跨设备数据同步，可以考虑：
1. **云数据库**：使用微信云开发数据库
2. **实时同步**：结合云函数实现多端同步
3. **用户系统**：添加用户登录功能，实现个人数据隔离
