# 微信云开发配置指南

## 📋 前置准备

在开始配置之前，请确保您已完成以下准备：

1. ✅ 已注册微信小程序账号
2. ✅ 已获取 AppID（您的项目已有：`wx804490de1f04dd56`）
3. ✅ 已安装微信开发者工具

## 🚀 配置步骤

### 第1步：开通云开发

1. **打开微信开发者工具**
2. **导入项目** 或 **打开现有项目** (`e:\codebuddy`)
3. **点击工具栏的"云开发"按钮**（位于工具栏顶部）
4. **点击"开通"按钮**
5. **选择环境配置**：
   - 环境名称：`codebuddy-dev`（可自定义）
   - 环境类型：**基础版**（免费）
   - 计费方式：按量付费
6. **点击"开通"**
7. **等待开通完成**（通常1-2分钟）

### 第2步：获取环境ID

开通成功后：

1. **点击"云开发"** 进入云开发控制台
2. **查看页面顶部**，会显示一个 `环境ID`
3. **复制环境ID**，格式类似：`cloud1-7gxxxxx`
4. **保存这个ID**，后面配置会用到

### 第3步：更新项目配置

打开 `e:\codebuddy/app.js` 文件，找到第19行：

```javascript
env: 'your-env-id', // 这里填写你的云环境ID
```

将 `your-env-id` 替换为您刚刚复制的环境ID：

```javascript
env: 'cloud1-7gxxxxx', // 替换为实际的环境ID
```

同时更新第21行：

```javascript
envId: 'cloud1-7gxxxxx' // 替换为实际的环境ID
```

### 第4步：上传云函数

1. **在微信开发者工具中**
2. **在左侧文件树找到 `cloudfunctions` 文件夹**
3. **右键点击 `getCourses` 文件夹**
4. **选择"上传并部署：云端安装依赖（云端安装依赖）"**
5. **等待上传完成**（首次需要安装依赖，可能需要1-2分钟）

同样方式上传其他云函数：
- `saveCourse`

### 第5步：创建数据库集合

1. **在云开发控制台**（点击"云开发"）
2. **选择"数据库"标签**
3. **点击"+"创建集合**
4. **创建以下集合**：

#### 集合1：courses（课程资源表）
- 集合名称：`courses`
- 权限设置：**所有用户可读，仅创建者可写**

**字段说明**：
```
_id: String (自动生成)
name: String (课程名称)
description: String (课程描述)
category: String (课程分类)
tags: Array (标签数组)
materials: Array (学习资料数组)
status: String (状态：active/archived)
createTime: Date (创建时间)
updateTime: Date (更新时间)
```

#### 集合2：homeworks（作业练习表）
- 集合名称：`homeworks`
- 权限设置：**所有用户可读，仅创建者可写**

**字段说明**：
```
_id: String (自动生成)
courseId: String (课程ID)
courseName: String (课程名称)
title: String (作业标题)
description: String (作业描述)
deadline: Date (截止日期)
isCompleted: Boolean (是否完成)
isOverdue: Boolean (是否过期)
createTime: Date (创建时间)
updateTime: Date (更新时间)
```

### 第6步：配置存储空间（可选）

如果您需要上传图片或文件：

1. **在云开发控制台**
2. **选择"存储"标签**
3. **点击"新建文件夹"**
4. **创建文件夹**：
   - `course-materials`（课程资料）
   - `homework-files`（作业附件）

### 第7步：测试配置

#### 测试1：检查云开发初始化

在微信开发者工具控制台中，应该看到：
```
课程助教小程序启动
```

#### 测试2：测试云函数调用

在任意页面的 JS 文件中添加测试代码：

```javascript
// 测试云函数
wx.cloud.callFunction({
  name: 'getCourses',
  data: {}
}).then(res => {
  console.log('云函数调用成功:', res)
}).catch(err => {
  console.error('云函数调用失败:', err)
})
```

运行后，在控制台查看返回结果。

## 📊 免费额度说明

微信云开发免费版每月提供：

| 资源类型 | 免费额度 | 说明 |
|---------|---------|------|
| 数据库读操作 | 50,000 次 | 查询数据 |
| 数据库写操作 | 30,000 次 | 添加/更新/删除数据 |
| 文件存储 | 5 GB | 图片、文件等 |
| CDN流量 | 5 GB | 文件下载流量 |
| 云函数调用 | 200,000 次 | 云函数执行 |
| 外网出流量 | 1 GB | 数据传输 |

对于学习和小型项目，这些额度完全够用！

## 🔧 常见问题

### Q1: 云函数上传失败
**A**: 检查以下几点：
- 确保已开通云开发
- 确保环境ID填写正确
- 尝试"云端安装依赖"选项

### Q2: 数据库权限错误
**A**: 在云开发控制台 → 数据库 → 集合 → 权限设置，改为：
- **所有用户可读**
- **仅创建者可写**

### Q3: 云函数调用超时
**A**:
- 检查网络连接
- 检查云函数代码是否有错误
- 查看云开发控制台的日志

### Q4: 如何查看云函数日志
**A**: 云开发控制台 → 云函数 → 选择函数 → 日志

### Q5: 如何重置数据库
**A**: 云开发控制台 → 数据库 → 选择集合 → 清空数据

## 📝 配置检查清单

- [ ] 已开通云开发
- [ ] 已获取并填写环境ID
- [ ] 已上传所有云函数
- [ ] 已创建所有数据库集合
- [ ] 已设置正确的数据库权限
- [ ] 已测试云函数调用
- [ ] 已测试数据库读写操作

## 🎯 下一步

配置完成后，您可以：

1. **修改前端代码**，将模拟数据改为调用云函数
2. **添加更多云函数**，实现学习问答等功能
3. **配置云存储**，实现资料上传功能
4. **设置数据库索引**，优化查询性能

## 📚 参考文档

- [微信云开发官方文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)
- [云函数开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/guide/functions.html)
- [数据库开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/guide/database.html)

---

**配置成功后，您的小程序就拥有完整的后端服务了！** 🎉
